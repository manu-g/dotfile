#!/bin/sh

echo "shutdown \t\t Shift + Win + p \t\t danach einen der angezeigten befehle ausführen"
echo "programm öffnen \t Alt + q \t\t\t öffnet kupfer"
echo "worspace wechseln \t win + 1, win + 2, ... \t "
echo "fenster schliesen \t win + shift + q \t "
echo "monitor einstellungen \t win + alt + m \t\t\t zeigt auswahl an voreingestellten configurationen an; -> alternative arandr über kupfer öffnen"
echo "touchpad einschalten \t win + t \t\t\t (o) == on, (f) == off"

